import 'package:flutter/material.dart';

class ScheduleWorkSelectPlacePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.blueAccent,
        title: Text('Chọn địa điểm'),
      ),
      body: Container(
        
      ),
    );
  }
}